package nz.aucklanduni.dfs;

public class DeptFirstSearch {

	public static void main(String[] args) {
		/**	
		private static List<SubModel> intialSplit(List<ComponentConfig> original){
			List<SubModel> result = new ArrayList<SubModel>();
			Stack<ComponentConfig> visitedStack = new Stack<ComponentConfig>();
			
			
			
			// find start component 
			for(int i=0; i<original.size(); i++ ) {
				if(original.get(i).isIsstartcaller()) {
					visitedStack.add(original.get(i));
					break;
				}
			}
			
			// traverse through graph
			List<ComponentConfig> visitedList = new ArrayList<ComponentConfig>();
			
			while(!visitedStack.isEmpty()) {
				ComponentConfig comp = visitedStack.pop();
				visitedList.add(comp);
				if(comp.getCalls().length>0) {
				}
			}
			
			return result;
		}
	**/	

	}

}
